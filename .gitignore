# RedPen ledger lives here; never commit a project's local verdict history.
.redpen/

# Personal Claude Code settings (where `redpen install-hook` writes its hook).
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/
.ruff_cache/

# OS / editor
.DS_Store
*.swp
