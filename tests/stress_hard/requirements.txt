# Extra dependencies for the tests/stress_hard adversarial suite.
#
# Installed into the project venv on demand; intentionally kept OUT of
# pyproject.toml so this suite stays fully isolated from the main package and
# from the separate tests/stress harness (which may be edited concurrently).
#
#   .venv/bin/python -m pip install -r tests/stress_hard/requirements.txt
hypothesis>=6.0
pytest-xdist>=3.0
faker>=20.0
pytest>=8.0
